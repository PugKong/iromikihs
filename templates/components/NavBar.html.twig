<nav id="navbar" class="bg-base-300">
  <div class="navbar lg:container lg:mx-auto">
    <div class="navbar-start">
      <div class="dropdown">
        <label tabindex="0" class="btn btn-ghost lg:hidden">
          {% include 'icons/navbar_dropdown.svg.twig' %}
        </label>
        <ul tabindex="0" class="z-[1] menu dropdown-content bg-base-300 rounded-box shadow-lg shadow-base-300 mt-4">
          {% for item in this.items %}
            <li>
              <a href="{{ item.path }}" class="{{ html_classes({'active': item.active}) }}">
                {{ item.label }}
                {% if item.count is defined %}
                  <span class="badge badge-accent">{{ item.count }}</span>
                {% endif %}
              </a>
            </li>
          {% endfor %}
        </ul>
      </div>
      <a href="{{ path('app_anime_index') }}" class="btn btn-ghost normal-case text-xl">Iromikihs</a>
    </div>

    <div class="navbar-center hidden lg:flex">
      <ul class="menu menu-horizontal">
        {% for item in this.items %}
          <li>
            <a href="{{ item.path }}" class="{{ html_classes({'active': item.active}) }}">
              {{ item.label }}
              {% if item.count is defined %}
                <span class="badge badge-accent">{{ item.count }}</span>
              {% endif %}
            </a>
          </li>
        {% endfor %}
      </ul>
    </div>

    <div class="navbar-end"><a href="{{ path('app_logout') }}">Log out</a></div>
  </div>
</nav>
